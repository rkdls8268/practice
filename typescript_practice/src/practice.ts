let count = 0;
count += 1;
// count = 'string'; // 에러 발생

const message: string = 'hello typescript'; // : string 이런식으로 타입 지정

const done: boolean = true;

// 타입 지정 안해줘도되나?
const numbers = [1, 2, 3];
const msg1 = 'str';

console.log(msg1);